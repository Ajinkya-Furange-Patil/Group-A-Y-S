"""
Scanner Modules Package

Contains all 8 scanner modules:
  MODULE 01 — SystemScanner   : Host machine metadata + Windows Copilot registry
  MODULE 02 — FileScanner     : AI model files on disk
  MODULE 03 — ProcessScanner  : Running AI processes and background daemons
  MODULE 04 — PackageScanner  : pip / NPM global / pipx / uv / Homebrew / conda
  MODULE 05 — AgentScanner    : AI agents in source code
  MODULE 06 — RuntimeScanner  : Active AI runtimes (ports & folders)
  MODULE 07 — APIScanner      : AI API key configurations
  MODULE 08 — MCPScanner      : Model Context Protocol config file parser
"""
