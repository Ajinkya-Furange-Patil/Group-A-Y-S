# AI Discovery Scanner — Pinned Dependencies
# Install with: pip install -r requirements.txt
#
# Last tested with Python 3.12 / 3.14 on Windows 11

# System introspection (process, CPU, RAM, disk, network)
psutil==7.2.2

# HTML report templating (Jinja2 + MarkupSafe dependency)
Jinja2==3.1.6
MarkupSafe==3.0.3

# ── Developer C: Optional export / reporting enhancements ──────────────────
# PDF export from HTML (optional — enable for --export-pdf flag)
# weasyprint==62.3        # cross-platform PDF from HTML
#
# Excel SBOM export (optional — enables Excel output alongside CSV)
# openpyxl==3.1.5         # xlsx writing for SBOM Excel exports

# ── Standard Library Modules used (no install needed) ──────────────────────
# sqlite3   — 180-day ICT log retention database
# csv       — SBOM CSV export
# socket    — Syslog SIEM transport
# urllib    — HTTP SIEM webhook transport

# ── Optional: EXE Packaging (not needed at runtime) ────────────────────────
# pyinstaller==6.12.0   # Uncomment to build dist/ai_scanner.exe
